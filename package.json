{
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.50.0"
  },
  "scripts": {
    "build": "./node_modules/.bin/browserify assets/main.js -o dist/main.js -t [ babelify --presets [ es2015 ] ] -g  uglifyify ",
    "prepublish": "yarn run build",
    "watch": "./node_modules/.bin/watchify assets/main.js -o dist/main.js -t [ babelify --presets [ es2015 ] ]"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "uglifyify": "^4.0.2",
    "watchify": "^3.9.0"
  }
}
